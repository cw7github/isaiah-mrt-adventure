# Copy to `.env.local` for local development (do not commit `.env.local`).

# ===== ElevenLabs TTS (serverless: /api/tts) =====
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=lqydY2xVUkg9cEIFmFMU
ELEVENLABS_MODEL_ID=eleven_v3
# Optional: mp3_44100_128, pcm_24000, etc.
ELEVENLABS_OUTPUT_FORMAT=

# ===== /api/tts hardening (recommended) =====
# Allowlist the web origins allowed to call /api/tts from a browser.
# You can set either:
# - `TTS_ALLOWED_ORIGINS` (comma-separated exact origins), OR
# - `TTS_ALLOWED_ORIGIN_REGEX` (regex tested against the Origin header)
#
# Examples:
# TTS_ALLOWED_ORIGINS=http://localhost:3000,https://isaiah-mrt-adventure.vercel.app
# TTS_ALLOWED_ORIGIN_REGEX=^https?://(localhost(:\\d+)?|isaiah-mrt-adventure(?:-[a-z0-9-]+)?\\.vercel\\.app)$
TTS_ALLOWED_ORIGINS=
TTS_ALLOWED_ORIGIN_REGEX=

# Best-effort rate limiting + in-memory caching (serverless warm instances only).
TTS_RATE_LIMIT_PER_MINUTE=30
TTS_CACHE_TTL_MS=300000
TTS_CACHE_MAX_ENTRIES=40

# ===== Firebase (client auth + Firestore via /api/firebase-config) =====
# These are the public Firebase web app identifiers (not secrets).
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_APP_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_MEASUREMENT_ID=
